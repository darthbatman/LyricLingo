#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lyrics-fetcher/bin/lyr" "$@"
  ret=$?
else 
  node  "$basedir/../lyrics-fetcher/bin/lyr" "$@"
  ret=$?
fi
exit $ret
